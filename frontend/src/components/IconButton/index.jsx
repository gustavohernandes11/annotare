import * as Styled from './styles.js'

export const IconButton = ({ width = 35, heigth = 35, children }) => {
    return (
        <Styled.Container>
            {children}
        </Styled.Container>
    )
}


